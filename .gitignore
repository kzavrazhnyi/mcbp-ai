# Existing project entries (preserved)
# Claude local settings (never commit)
.claude/settings.local.json
.claude_dev/settings.local.json

# Python
backend/.venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.so

# Secrets
.env
.env.*
*.key
*.pem
*.crt
*.p12
credentials*

# Logs & temp
*.log
logs/
tmp/
.scripts/

# BAS vendor — 1C IP
bas/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# pytest
.pytest_cache/
.coverage
htmlcov/

# Ruff
.ruff_cache/

# Claude Code configuration (local, not tracked)
# Shipped by the claude_setup generator. Per-developer Claude tooling stays out
# of the shared history so two configs never conflict in one repo.
# If your team decides to track any of these in git, delete its line below.

# end Claude Code configuration
